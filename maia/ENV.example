# =============================================================================
# Maia Configuration Template
# =============================================================================
# Maia inherits configuration from Atlas. Copy this file to .env and configure.
#
# IMPORTANT: All configuration is managed by Atlas. Maia does NOT have its own
# configuration layer - it uses atlas.config.settings exclusively.
#
# See Atlas documentation for detailed configuration guide.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql://user:password@host:5432/pleiades

# -----------------------------------------------------------------------------
# Storage Provider Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Options: huggingface | gcs
VAULT_PROVIDER=huggingface

# HuggingFace Configuration (if VAULT_PROVIDER=huggingface)
HF_DATASET_ID=your-username/your-dataset
HF_TOKEN=hf_your_token_here

# Google Cloud Storage Configuration (if VAULT_PROVIDER=gcs)
# GCS_BUCKET_NAME=your-bucket-name
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# -----------------------------------------------------------------------------
# YouTube API Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Provide API keys as a JSON array
# For multiple keys: YOUTUBE_API_KEY_POOL_JSON='["key1", "key2", "key3"]'
# For single key: YOUTUBE_API_KEY_POOL_JSON='["AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"]'
YOUTUBE_API_KEY_POOL_JSON=["AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"]

# -----------------------------------------------------------------------------
# Key Ring Allocation (REQUIRED)
# -----------------------------------------------------------------------------
# Functional key pooling: Separate keys for different agents
# Total keys needed: HUNTING + TRACKING + ARCHEOLOGY
# Minimum 3 keys recommended for separation
#
# Hunter uses: All keys except those reserved below
# Tracker uses: KEY_POOL_TRACKING_SIZE keys
# Archeology (future): KEY_POOL_ARCHEOLOGY_SIZE keys
KEY_POOL_ARCHEOLOGY_SIZE=1
KEY_POOL_TRACKING_SIZE=1

# -----------------------------------------------------------------------------
# Compliance & Environment (OPTIONAL)
# -----------------------------------------------------------------------------
# COMPLIANCE_MODE: If true, limits to single API key (standard quota mode)
COMPLIANCE_MODE=true

# ENV: Deployment environment tag
ENV=dev

# -----------------------------------------------------------------------------
# Discord Notifications (OPTIONAL)
# -----------------------------------------------------------------------------
# Webhook URLs for different alert channels
# DISCORD_WEBHOOK_ALERTS=https://discord.com/api/webhooks/...
# DISCORD_WEBHOOK_HUNT=https://discord.com/api/webhooks/...
# DISCORD_WEBHOOK_SURVEILLANCE=https://discord.com/api/webhooks/...
# DISCORD_WEBHOOK_OPS=https://discord.com/api/webhooks/...

# -----------------------------------------------------------------------------
# Prefect Orchestration (OPTIONAL)
# -----------------------------------------------------------------------------
# Configure if using Prefect Cloud for workflow orchestration
# PREFECT_API_URL=https://api.prefect.cloud/api/accounts/.../workspaces/...
# PREFECT_API_KEY=pnu_...




